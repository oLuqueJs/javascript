// basic prompt, every prompt returns a string
const num1 = parseInt(prompt('Digite um número'));
const num2 = parseInt(prompt('Digite outro número'));

let soma = num1 + num2
let mult = num1 * num2

alert(`A soma dos dois números é ${soma}, e o produto dos dois números é ${mult}`)